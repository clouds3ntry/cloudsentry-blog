---
export interface Props {
	tag: string
}
const { tag } = Astro.props
---

<a href={`/tags/${encodeURIComponent(tag.toLowerCase())}`} aria-label={tag}>
	<span class='nav-pill text-sm font-medium hover:scale-105 transition-all duration-200'>
		{tag ?? 'Blog Tag'}
	</span>
</a>
